
@model IEnumerable<GameAndMods.Models.Users>

@{
    ViewBag.Title = "Добавление пользователя";
    Layout = null;
}
<div class="container">
    <form asp-action="Create" asp-controller="Home" asp-antiforgery="true" method="post" enctype="multipart/form-data">
        <div class="validation" asp-validation-summary="ModelOnly"></div>
        <p>
            <label>Email</label>
            <input name="Email" type="email" class="form-control" />
        </p>
        <p>
            <label>Password</label>
            <input name="Password" type="password" class="form-control" />
        </p>
        <p>
            <label>Аватар</label>
            <input name="Avatar" type="file" class="form-control" />
        </p>
        <p>
            <input type="submit" value="Добавить" />
        </p>
    </form>
    @*<h3>Все пользователи</h3>
    @foreach (var p in Model)
    {
        <div>
            <h4>@p.Email</h4>
            @if (p.Avatar != null)
            {
                <img style='width:80px; height:60px;' src="data:image/jpeg;base64,@(Convert.ToBase64String(p.Avatar))" />
            }
        </div>
    }
</div>*@
